## Quick Routing

Use this table first. Match your situation to a scenario and run the wiki-query before answering.

---

> **Note: Zero routing entries generated — KB gap.**
>
> This routing table was generated by `momentum:constitution-builder` for a React + Next.js skill covering React hooks (useState, useEffect, useCallback, useMemo), Next.js App Router, React Server Components, and data fetching patterns (fetch, SWR, React Query).
>
> The wiki KB at `/Users/steve/projects/nornspun-agentic-kb` contains no pages covering any of these domains. All four technology areas audited are **gaps** — no KB backing exists for any routing entry.
>
> Per constitution-builder design: "gaps in the KB mean gaps in routing." Generating fabricated `wiki-query` invocations for uncovered concepts would be dishonest and would cause the skill to return empty results at runtime.
>
> **Recommended next step:** Invoke `wiki-ingest` or `wiki-research` for the following concepts before regenerating this routing table:
> - React hooks — useState, useEffect, useCallback, useMemo, useRef, useContext
> - Next.js App Router — layouts, loading.js, error.js, route handlers, parallel routes
> - React Server Components — the server/client boundary, serialization rules, use client/use server
> - Data fetching patterns — fetch with caching in RSC, SWR hook API, React Query (TanStack Query) patterns
> - Loading and error state patterns — Suspense boundaries, error boundaries, streaming SSR

### KB Coverage Audit

| Technology | Coverage | Notes |
|---|---|---|
| React hooks (useState, useEffect, useCallback, useMemo) | **Gap** | No pages found in KB |
| Next.js App Router | **Gap** | No pages found in KB |
| React Server Components | **Gap** | No pages found in KB |
| fetch-based data fetching (RSC) | **Gap** | No pages found in KB |
| SWR | **Gap** | No pages found in KB |
| React Query / TanStack Query | **Gap** | No pages found in KB |

### What Would Be Here After Ingest

For reference, the routing entries this table would contain once the KB is populated:

**React Hooks — State and Effects**
- Hook runs every render even though dependency list looks correct → `wiki-query why does useEffect run on every render despite dependencies`
- useEffect cleanup not firing causing memory leaks or stale subscriptions → `wiki-query useEffect cleanup function when does it run`
- useState setter not reflecting new value immediately inside the same render → `wiki-query useState async update why value unchanged after set`
- Choosing between useState and useReducer for complex state → `wiki-query quick answer: useState vs useReducer when to use each`

**React Hooks — Performance**
- Component re-renders too often, need to memoize a value → `wiki-query useMemo when to use and performance cost`
- Passing a callback to a child without causing re-render every time → `wiki-query useCallback dependency array and referential stability`
- useMemo or useCallback dependencies triggering recalculation unexpectedly → `wiki-query useMemo dependency comparison referential equality objects`
- Deciding whether a memoization optimization is worth adding → `wiki-query useMemo useCallback premature optimization when not to use`

**Next.js App Router — Server vs Client Components**
- Need interactivity (hooks, events) but getting "hooks can only be called in client components" error → `wiki-query Next.js use client directive when required`
- Unsure whether a new component should be server or client → `wiki-query React Server Components decision tree server vs client`
- Passing data from a Server Component to a Client Component → `wiki-query RSC serialization rules what can be passed as props to client`
- Client component trying to access server-only resources (DB, secrets) → `wiki-query server component data access patterns keeping secrets server-side`

**Next.js App Router — Routing and Layouts**
- Shared layout across routes without re-mounting on navigation → `wiki-query Next.js App Router layout.js persistent layout`
- Per-route loading state while data fetches → `wiki-query Next.js loading.js Suspense boundary file convention`
- Catching errors at route level without crashing whole app → `wiki-query Next.js error.js error boundary route segment`
- Setting up a route that handles API requests (replacing pages/api) → `wiki-query Next.js route handlers App Router GET POST`

**Data Fetching — RSC and Client Patterns**
- Fetching data in a Server Component with caching → `wiki-query Next.js fetch cache revalidate in React Server Components`
- SWR not refetching when expected, or refetching too aggressively → `wiki-query SWR revalidation options dedupingInterval focus`
- React Query cache stale and need to force refetch → `wiki-query React Query staleTime refetch invalidateQueries`
- Managing loading and error states with React Query → `wiki-query TanStack Query isLoading isError status patterns`
- Parallel data fetching to avoid request waterfalls → `wiki-query React Server Components parallel fetch Promise.all waterfall`
