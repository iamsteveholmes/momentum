# Handoff: DEC-029 Triage Batch — Run in Fresh Session

**Date:** 2026-05-17
**Branch:** sprint/sprint-2026-05-16
**Purpose:** Resume and run `momentum:triage` on the work items implied by DEC-029, in a clean session on Opus.

---

## State (done this session)

- **DEC-029 written, committed (`d19b07e`), registry updated.**
  `_bmad-output/planning-artifacts/decisions/dec-029-method-routed-acceptance-validation-pipeline-2026-05-17.md`
  Method-routed acceptance validation; per-story frozen contract as spec-of-done; per-project `momentum/harness.json` (sibling to `agents.json`); per-sprint holistic E2E coverage plan with transitive coverage; unified validate-fix loop (code-review/AVFL/E2E as one primitive); "process document" artifact class dissolved; sprint-dev rewrite sequenced as a separate downstream epic.
- **Triage skill model changed to Opus** in the repo: `skills/momentum/skills/triage/SKILL.md` → `model: claude-opus-4-7` (was `claude-sonnet-4-6`). Commit pending in this handoff's commit.
- Memory updated: `feedback_process_as_enforced_rule`, `feedback_decision_to_backlog_bridge`, `project_e2e_validation_redesign`.

## Before starting the new session

The triage skill executes from the **plugin cache**, not the repo. The Opus model change will NOT take effect until the cache is refreshed:

1. `/plugin marketplace update momentum`
2. Restart the session (full restart — `/reload-plugins` is insufficient; see `feedback_fresh_session_before_major_workflows`).
3. Confirm: `skills/.../triage/SKILL.md` in the cache shows `model: claude-opus-4-7`.

## Then run

Invoke `momentum:triage` with this exact batch (all items reference DEC-029 as source/rationale; **do not create stories — classify and queue only; stories are created at sprint-planning**):

**Net-new Phase 1 work items to queue:**
1. Enforced verification rule replacing retired `acceptance-testing-standard.md` — method-routing by change-type (D1), harness-profile requirement (D3), adversarial anti-insider-knowledge guard (D6); cascades global→project→path-scoped. (DEC-029 D1/D3/D6/D7)
2. `momentum/harness.json` schema + plugin-shipped defaults block — sibling to `momentum/agents.json`, same defaults/project shape, JSON; env startup+readiness probes, execution surface per change-type, driver binding (cmux/Skill/Maestro/Playwright/curl), platform/target matrix, human-review carve-outs, trivial-smoke escape; written by agent-builder/agent-guidelines. (DEC-029 D3)
3. `e2e-validator` agent body rewrite — de-Gherkin (consume method-polymorphic contracts, not only `.feature`), remove hardcoded finch/PostgreSQL/FastAPI leak, drive from `momentum/harness.json`. (DEC-029 D1/D3; affects DEC-020)
4. `create-story` method-selection step — owns choosing verification method from change-type routing; escalates to developer when ambiguous. (DEC-029 D5)
5. `sprint-planning` contract + coverage-plan authoring + adversarial guard — authors frozen method-polymorphic per-story contract in `.momentum/sprints/{slug}/specs/` AND the per-sprint holistic E2E coverage plan (scenario→story/file span map, transitive coverage, anti-redundancy); runs the adversarial anti-insider-knowledge guard. (DEC-029 D2/D6/D8)

**Existing stories — classify as "needs refine/rescope" observation, do NOT recreate:**
- `acceptance-testing-process-and-standards` (standard retired by D7 → becomes the enforced rule)
- `change-type-routing-in-sprint-dev` (extends to verification-method routing per D5)
- e2e-validator cluster: `e2e-validator-black-box-hardening`, `e2e-validator-toolsearch-fix`, `e2e-and-qa-validator-prompts-branch-standalone-vs-team`, `e2e-client-side-coverage`, `e2e-validator-fixture-spec-sync-stale-reference-detection` — likely superseded/rescoped by the harness-driven rewrite; needs a refine pass.

**Explicitly OUT of scope (do not queue):**
- Full sprint-dev rewrite — DEC-029 Phase 2, sequenced as its own separate downstream decision/epic.
- Beads-backed state ledger — DEC-029 Phase 3, gated on DEC-028 spike verdict.

## Dependency / gating reminder

Phase 1 build is gated on `routing-table-schema-and-implementation` landing `momentum/agents.json` (DEC-029 Gate 1). Triage/queue now is fine; implementation sequencing respects that gate.

## Side note for later (not this batch)

The skill dev guide (`skills/momentum/references/agent-skill-development-guide.md`) documents the model field as short form (`opus|sonnet|haiku`), but all skills in `skills/momentum/skills/*` use full IDs (`claude-opus-4-7`, `claude-sonnet-4-6`). Triage was set to `claude-opus-4-7` for parity with siblings. Guide-vs-practice discrepancy worth its own triage item.
